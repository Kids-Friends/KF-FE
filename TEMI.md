# Temi Robot — Developer Reference (KR)

> **목적**: 테미(temi) 플랫폼 위에서 앱/서비스를 개발하는 AI·에이전트가 빠르게 조회하기 위한 도메인 지식 정리본.
> **출처**: 테미 한국어 사용설명서 ver 1.0 (2019.09)
> **주의**: 본 문서는 *사용설명서 기반의 동작/제약 정보*이며, Temi SDK(API 시그니처) 문서가 아님. SDK 호출부는 별도 확인 필요(아래 §19 참고). 매뉴얼에 없는 값은 추정하지 않고 `미기재`로 표기.

---

## 1. 제품 한 줄 요약

테미는 **Alexa 내장 음성 비서 + 자율주행 모빌리티 + 핸즈프리 영상통화(telepresence)** 를 합친 퍼스널 로봇. 3대 핵심 = `AI` / `Video(영상통화)` / `Mobility`.

개발 관점에서 중요한 3축:
- **자율주행/매핑**: LIDAR + 16센서, 홈베이스를 지도 기준점으로 사용
- **위치(Location) 시스템**: 장소 저장/이동/삭제 → 텔레프레즌스·자동주행의 핵심
- **사용자/권한 모델**: 소유자(1명)/회원/비회원 3단 권한

---

## 2. 하드웨어 구성

전체 스펙은 공식 사이트(`www.robotemi.com/specs`)에서 확인. 매뉴얼에 명시된 구성요소:

### 2.1 헤드(상단)
| 구성요소 | 비고 |
|---|---|
| 주행 / 사진 / 동영상 카메라 | 상단 단일 카메라(주행+촬영 겸용) |
| 3D Depth 카메라 | 깊이 인식 |
| 얼굴 추적 카메라 | 동행(follow) 모드용 |
| 마이크 | 음성 인식 |
| 스피커 | 트위터 2EA + 미드레인지 2EA |
| LED 표시등 | 전원/상태 |
| 동작(Interaction) 버튼 | follow 모드 시작·정지 트리거 |
| 볼륨 조절 버튼(+/−) | 길게 누르면 화면 밝기 바 표시 |

### 2.2 베이스(하단)
| 구성요소 | 비고 |
|---|---|
| LIDAR 센서 | 매핑/내비게이션 핵심, 절전 시 회전 정지 |
| 3D Safety Depth 카메라 | 충돌 방지 |
| 범위 센서 | |
| 보조 우퍼 | |
| 드라이빙 휠 / 캐스터 휠 | 구동/지지 |
| 충전 판 / 접촉부 | 홈베이스 도킹 |

### 2.3 트레이 (Qi 무선 충전)
| 항목 | 값 |
|---|---|
| 무선 충전 | Qi 인증 패드 내장 |
| **트레이 최대 적재 (충전 섹션 기준)** | **2.7 kg** |
| 적재 한계 (안전 일러스트 기준) | 2.5 kg |

> ⚠️ **불일치 주의**: 본문(Qi 트레이 설명)은 **2.7kg**, 후반 안전 일러스트는 **2.5kg**으로 다르게 기재됨. 보수적으로 **2.5kg 이하**로 가정 권장.

### 2.4 홈베이스
- 입력: 100–240V / 50/60Hz 표준 콘센트
- 지도/위치 복구 기준점 역할(도킹 위치)
- 전력 변환기(트랜스포머) 사용 시 보증 무효

---

## 3. 전원 / 절전 동작 (개발 시 타이밍 영향)

| 항목 | 값/동작 |
|---|---|
| 완충 연속 사용 | 평균 **8시간** |
| 배터리 부족 | 화면 경보 + "홈베이스로 보내기" 버튼 표시 |
| 전원 ON | 전원 버튼 1회 누름 → LED 점등 |
| 전원 OFF | LED 꺼질 때까지 **3초** 길게 누름 |
| **LIDAR 절전** | **5초 이상 무동작 시 LIDAR 회전 정지** |
| **LIDAR 재가동(spin-up)** | follow 모드 시작 시 **3~4초** 재회전 필요 |

> **개발 함의**: 정지 상태에서 곧장 자율주행/follow를 호출하면 LIDAR 재가동 지연(3~4초)이 존재. 주행 명령 직후 즉시 정밀 회피를 기대하지 말 것.

---

## 4. 매핑 & 내비게이션

- LIDAR로 **활동 중 매초 지도 갱신**(실시간 재매핑).
- 16개 센서 + 전용 CPU로 장애물/가구/사람 구분.
- 자동 내비게이션 모드: **동행 / 이동 / 순찰 / 영상통화**.

### 4.1 회피 불가 장애물 (중요 제약)
다음은 회피 **불가** → 테스트/시나리오 설계 시 반드시 회피 책임을 외부 로직 또는 환경 통제로 보완:
- **완전한 검은색** 물체
- **높이 10cm 미만**의 낮은 물체

### 4.2 안전 거리
- 주행 중 주변 **최소 1m** 안전거리 확보.
- 계단/선반/경사/낭떠러지 등 추락 위험 방향 유도 금지(특히 follow 모드).

---

## 5. 동행(Follow / 동행 모드) 동작 모델

활성화: **로봇 앞에 서서 헤드의 동작(Interaction) 버튼 누름**.
상태 표시: 화면 상단 `Searching(탐색 중)` → `Following(따라가기)`.

### 5.1 거리별 동작 (상태머신처럼 활용 가능)
| 사용자 거리 | 테미 동작 |
|---|---|
| **< 1.2m** | 전진하지 않고 사용자 방향만 바라봄(회전) |
| **1.2 ~ 2.5m** | 사용자에게 주행하며 최적 거리 유지 |
| **> 2.5m** | 사용자가 가까워질 때까지 대기 |

### 5.2 버튼 구분 (혼동 주의)
| 버튼 | 동작 |
|---|---|
| 동작(Interaction) 버튼 가볍게 | follow 모드 시작 |
| **전원 버튼 누름** | **개인정보(오프라인) 모드 진입** — follow 아님! |

### 5.3 정지
- "헤이 테미, 멈춰" 또는 동작 버튼 누름.
- 미정지 시 전원 버튼 길게 눌러 강제 종료.

---

## 6. 장소(Location) 시스템

집/사무실 내 위치 저장 → 영상통화 중 또는 자동주행으로 해당 위치 이동.

### 6.1 테미 본체 조작
| 작업 | 음성 | 터치(상단 바) |
|---|---|---|
| 저장 | "헤이 테미, 여기를 주방으로 저장해" | 위치 아이콘 > 새 위치 설정 > 이름 입력 |
| 이동 | "헤이 테미, 주방으로 가" | 위치 아이콘 > 위치 이름 선택 |
| 삭제 | — | 위치 아이콘 > … > 삭제할 장소 선택 |

### 6.2 모바일 앱(영상통화 중)
- 위치 아이콘으로 저장 위치 화면 진입 → `+` 로 새 위치 추가 → 이름 입력/자동입력 후 "저장".
- 저장 위치 아이콘 탭으로 해당 장소 이동.

> **개발 함의**: 저장 위치는 **홈베이스 기준 지도**에 종속. 홈베이스 이동/맵 리셋 시 저장 위치 전부 소실(§7 참고).

---

## 7. 홈베이스 & 지도 무결성 (가장 중요한 운영 제약)

홈베이스 = **지도의 기준점(map reference)**. 분리/이동 시 지도·위치 재설정 유발.

### 7.1 설치 권장 공간
| 항목 | 값 |
|---|---|
| 홈베이스 양쪽 여유 | 최소 45cm |
| 홈베이스 앞쪽 여유 | 최소 1m |
| 환경 | 평평·단단한 벽 앞, 거울/반사체·도킹 오인 유발물 없을 것 |

### 7.2 지도 경보 처리 (`위치를 사용할 수 없음` / Locations not available)
홈베이스에 도킹 후 안내 표시 → **분기 선택이 데이터 보존을 좌우**:
| 상황 | 선택 | 결과 |
|---|---|---|
| 홈베이스 **안 옮김** | **NO** | 지도·저장 위치 **복구** |
| 홈베이스 **옮김** | **YES** | 맵 **재설정 + 저장 위치 전부 삭제** |

### 7.3 수동 맵 재설정
- 내비게이션 오작동 감지 시 / 모든 저장 위치 일괄 삭제 시 권장.
- 절차: **홈베이스 도킹 후** `설정 > 지도 > 재설정(지도 초기화)`. (저장 위치 전부 삭제됨)

### 7.4 영상통화 중 지도 문제
- 통화 중에는 운전 불가. 통화 종료 후 홈베이스로 이동시켜 지도 복원.
- 핸드폰·테미 양쪽에 경고 표시.

---

## 8. 영상통화(Telepresence) & 원격 제어

### 8.1 통화 시작
- 테미: "홍길동에게 전화해" 또는 화면 터치 > 연락처 선택.
- 모바일 앱: 연락처 선택 시 **로봇 연결 / 앱 연결** 선택 옵션.

### 8.2 등록 여부에 따른 수신 동작
| 호출자 | 동작 |
|---|---|
| 테미 등록 사용자(회원) | 수 초 내 **자동 연결**, 종료 버튼으로 해제 |
| 미등록 사용자 | 수신 측에서 **허락/거부** 선택 필요 |

### 8.3 모바일 앱 원격 제어 제스처 (통화 제어 뷰)
| 입력 | 동작 |
|---|---|
| 화면 1회 터치 | 해당 방향 바라보기(헤드 회전) |
| 화면 2회 터치 | 화면 확대(zoom) |
| 길게 눌러 흰색 물결무늬 | 해당 지점으로 이동 |
| 좌/우 드래그 | 해당 방향 90° 회전 |
| 위치 아이콘 | 저장 장소로 이동 |
| 조이스틱 | 원격 수동 운전 |

뷰 종류: `통화 제어 뷰` ↔ `로봇 제어 뷰`(좌하단 컨트롤 버튼으로 전환) ↔ `저장 장소 보기 뷰`.

### 8.4 통화 중 알림 상태
| 알림 | 의미 |
|---|---|
| 장애물(주황색) | 회피 불가 장애물로 원격 주행 시도 시 |
| 원격 조종 아이콘 | 원격 제어 중 상단바 표시 |
| 카메라 종료 | 원격 사용자 카메라 OFF |
| 재연결 중 | 연결 끊김 → 재연결 시도 |

---

## 9. 음성 비서 (테미 vs Alexa)

호출: 화면 좌상단 마이크 아이콘 탭 **또는** 웨이크워드.

| 비서 | 웨이크워드 | 명령어 레퍼런스 |
|---|---|---|
| temi | "헤이 테미" | `www.robotemi.com/skills` |
| Alexa | "알렉사" | `https://theassistant.io/commands` |

- 비서 전환: `설정 > 개인 비서`.
- Alexa 연결: `설정 > 개인 비서 > 알렉사 계정` → `Amazon.com/code`에 표시 코드 입력.

> ⚠️ **Alexa 사용 시 제약**: **카메라 / 위치 / 영상통화**는 Alexa 음성으로 실행 불가 → **화면 터치로 수동 실행 필요**. (매뉴얼 시점 기준, 추후 업데이트 예정으로 명시)

볼륨: 1–10. "헤이 테미, 볼륨 7로 바꿔줘" / 헤드 +/− 버튼. (Alexa도 음성 가능)

---

## 10. 내장 기능 / 스킬 (명령어 패턴)

| 기능 | 예시 명령 | 비고 |
|---|---|---|
| 지식 | "에펠 탑 높이가 어떻게 돼?" | Q&A |
| 날씨 | "오늘/내일/다음 주 ○○ 날씨 어때?" | 지역·기간 지정 가능 |
| 뉴스 | "오늘 뉴스 알려줘" | |
| YouTube | "유튜브에서 ○○ 재생해줘" | 광범위 검색 시 결과 리스트 |
| iHeartRadio | "iHeartRadio에서 ○○ 재생해줘" | 음악/팟캐스트/라디오 |
| Yelp | "서울시 광진구의 치킨집 찾아줘" | 주변 식당 |
| 카메라(사진) | "사진 찍어줘" | **3초 카운트다운 후 촬영** |
| 카메라(비디오) | "비디오 찍어줘" | **10초** 영상 |
| 카메라(GIF) | "GIF 찍어줘" | **사진 10장**으로 GIF 생성 |
| 타이머 | "10초 타이머 맞춰줘" | |
| 전화 | "엄마한테 전화 걸어줘" | telepresence |

촬영물(사진/영상/GIF)은 모바일 앱 **활동(Activity) 스트림**에서 확인.

---

## 11. 사용자 / 권한 모델

| 역할 | 인원 | 정의 |
|---|---|---|
| 소유자(Owner) | **1명 제한** | 활성화 시 QR 스캔한 사용자 |
| 회원(Member) | 다수 | 소유자가 초대 |
| 비회원(Non-member) | 다수 | 미등록 |

모든 사용자: 모바일 앱 설치 필수.

### 11.1 권한 매트릭스 (●=가능 / ✗=불가)
| 역할 | 테미(통화) | 동영상 | 위치 추가 | 활동 스트림 | 연락처 동기화 | 회원 관리 |
|---|:---:|:---:|:---:|:---:|:---:|:---:|
| 소유자 | ● | ● | ● | ● | ● | ● |
| 회원 | ● | ● | ● | ● | ✗ | ✗ |
| 비회원 | ● | ✗ | ✗ | ✗ | ✗ | ✗ |

### 11.2 회원/연락처 동기화 규칙
- 회원 추가: 모바일 앱 > "나의 테미" > **+ 회원 초대** > 연락처 선택 > 추가. (한 번에 다수 가능)
- 연락처 동기화 전제: 최신 앱 + **전화번호에 국가 접두사 포함**(한국 `+82`). 테미에 저장 안 된 번호로 로그인 시 해당 연락처 미표시.
- 로봇 표시 연락처 = **소유자의 앱 연락처와 동일**.
- 회원 본인 탈퇴: 앱에서 "탈퇴" 가능.
- **소유자(관리자) 삭제 = 공장 초기화 필수** → 전 데이터·전 회원 제거.

### 11.3 상태 아이콘
| 색 | 의미 |
|---|---|
| 회색 | 사용 불가 |
| 노란색 | 다른 사용자 사용 중 |
| 초록색 | 사용 가능 |

---

## 12. 모드 (Standby / Privacy / Offline)

| 모드 | 트리거 | 동작 |
|---|---|---|
| 대기(Standby) | ON 후 또는 30초 무사용 | 10초 뒤 화면보호기→검은 화면. **음성/터치는 계속 수신** |
| 일반설정 대기 | `설정 > 일반 설정` ON | 켜면 **10분 후 화면 어두워짐** + "음성으로 전환" 토글 노출 |
| 개인정보/오프라인 | **전원 버튼 눌렀다 놓기** (녹색선→빨간선) | **마이크·카메라 OFF, 인터넷 분리**, 제어는 터치만 |
| 오프라인 해제 | 작업(동작) 버튼 길게 누름(빨간선→녹색선) | 온라인 복귀 |

> **개발 함의**: 개인정보 모드 = 네트워크 단절 + 센서 차단. 서버 연동/원격 호출 로직은 이 상태에서 전부 실패 전제로 핸들링 필요.

---

## 13. 상단 메뉴 / 상태 아이콘

| 아이콘 | 의미 |
|---|---|
| 배터리 | 충전/잔량 |
| WiFi | 신호 강도 / 연결 문제 |
| 시스템 상태(경고 삼각형) | 현재 비활성 기능 확인 가능 |
| 연락처 | 연락처/영상통화 진입 |
| 위치 | 위치 메뉴 진입 |
| 홈/기능 | 홈 화면 ↔ 기능 화면 전환 |
| 영상통화 | 진행 중 통화 진입 |

> WiFi 미연결 시 **전체 기능 정상 동작 불가**. 초기 셋업 또는 `설정 > WiFi`에서 구성.

---

## 14. 설정(Settings) 경로 맵 (빠른 조회용)

| 목적 | 경로 |
|---|---|
| 새 테미 활성화 | `설정 > 새 테미 활성화` |
| 공장 초기화(관리 전환) | `설정 > 공장 초기화` |
| 음성 비서 변경 | `설정 > 개인 비서` |
| Alexa 연결 해제 | `설정 > 개인 비서 > 알렉사 계정 > 연결 해제` |
| WiFi 관리 | `설정 > WiFi` |
| 별명(닉네임) 변경 | `설정 > 테미의 닉네임` *(연락처 정보 변경됨)* |
| 지도 초기화 | `설정 > 지도 > 지도 초기화(재설정)` *(홈베이스 도킹 필요)* |
| 소프트웨어 업데이트 | `설정 > 소프트웨어 업데이트` |
| 대기 모드 토글 | `설정 > 일반 설정 > ON/OFF` |
| 시작 가이드 | `설정 > 시작` |
| 운전 방법 | `설정 > 운전하는 방법` |

---

## 15. 소프트웨어 업데이트

- 자동: 업데이트 가용 시 자동 다운로드/설치 메시지 표시.
- 수동: `설정 > 소프트웨어 업데이트`.

---

## 16. 개발자 도구 (Dev-relevant)

| 항목 | 내용 |
|---|---|
| 커스텀 앱 설치 | **ADB 포트 열기** 후 사용자 정의 앱 설치 |
| IP 주소 위치 | 화면 우측 코너에 표시 |
| 화면 밝기 | 헤드 **볼륨 버튼** 누르면 밝기 조절 바 노출 |

> 매뉴얼에는 ADB **WiFi 디버깅 포트/명령** 상세가 없음(`미기재`). 실제 `adb connect <IP>:<port>` 절차는 SDK/디바이스 설정에서 별도 확인 필요.

---

## 17. 물리/환경 제약 (테스트 환경 설계용)

| 항목 | 제약 |
|---|---|
| 작동 온도 | **0°C ~ 40°C** |
| 환경 | 실내 전용, 건조·평평·단단한 바닥 |
| 금지 | 실외, 가연성 가스/연기 환경, 직사광선·레이저(센서 손상), 털 많은 깔개·전선 많은 곳(휠/모터 손상) |
| 연령 | 13세 미만 단독 사용 금지 |
| 트레이 적재 | ≤ 2.5kg 권장(§2.3), 액체·뜨거운 음료·깨지기 쉬운 물체 금지 |
| follow 환경 | 실내 ✓ / 실외·계단·절벽 ✗ |

---

## 18. 개발 시 핵심 체크포인트 (요약)

1. **지도/위치는 홈베이스에 강하게 종속** — 홈베이스 이동 = 위치 데이터 소실. 앱에서 위치 의존 로직은 "맵 리셋" 이벤트를 반드시 핸들링.
2. **LIDAR 절전 spin-up 3~4초** — 정지 후 즉시 정밀 주행 호출 시 초기 회피 신뢰도 저하 고려.
3. **회피 불가 장애물(검은색/10cm 미만)** — 자율주행 시나리오에서 환경 통제 또는 외부 안전로직 필요.
4. **Alexa 비서일 때 카메라/위치/영상통화 음성 불가** — 어시스턴트 모드 분기 처리.
5. **권한 3단계** — 카메라/촬영(동영상)·위치·동기화·회원관리 권한이 역할별로 다름. 기능 게이팅 시 권한 매트릭스(§11.1) 기준.
6. **개인정보(오프라인) 모드 = 네트워크·센서 OFF** — 서버 연동 기능은 실패 전제 핸들링.
7. **telepresence 등록 사용자만 자동 연결**, 미등록은 수동 승인 — 통화 플로우 분기.
8. **트레이 적재 한계 표기 불일치(2.7 vs 2.5kg)** — 보수적으로 2.5kg.

---

## 19. SDK 연결 메모 (확인 필요 — 매뉴얼 외 영역)

> 아래는 매뉴얼 동작을 실제 개발과 잇기 위한 **연결 포인트 가이드**이며, 정확한 메서드명/시그니처는 공식 Temi Android SDK(`robotemi/sdk`) 문서로 검증할 것. 매뉴얼은 SDK API를 다루지 않으므로 본 절은 "어느 SDK 영역을 봐야 하는지"의 길잡이용.

| 매뉴얼 기능 | SDK에서 찾아볼 영역(추정) | 검증 필요 |
|---|---|---|
| 장소 저장/이동 | Location 관련 API, goTo 상태 리스너 | ✅ |
| follow 모드 | follow/beWith 류 API | ✅ |
| 영상통화 | telepresence 호출 API | ✅ |
| 배터리/상태 | battery / robot ready 콜백 | ✅ |
| 헤드 틸트/회전·수동주행 | tilt/turn/skidJoy 류 | ✅ |
| TTS/음성 | speak(TtsRequest) | ✅ |

---

*문서 끝. 추가로 KF(Kids-Friends) 프로젝트용으로 "Temi Android 앱 ↔ Spring Boot REST 연동 시 위치/통화 이벤트 매핑" 같은 통합 레퍼런스가 필요하면 별도 .md로 확장 가능.*
